spring:
  datasource:
    password: mypassword
    url: jdbc:postgresql://localhost:5437/mydatabase-test
    username: myuser
  jackson:
    serialization:
      write-dates-as-timestamps: false
  flyway:
    enabled: true
    baseline-on-migrate: false

mail:
  enabled: false
